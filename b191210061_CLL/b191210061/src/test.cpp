#include <cstdlib>
#include "AVLNode.hpp"
#include "AVL.hpp"
#include <iostream>

using namespace std;

int main()
{
	AVLNode* root=NULL;
    /*root = new AVLNode(2010,1,"Ahmet Yilmaz",'A');
    cout<<"Root: "<< root->year<<" "<<root->no<<" "<<root->data<<" "<<root->companyName<<endl;
    root->right=new AVLNode(2012,2,"Mehmet Sari",'B');
    cout<<"Right: "<<root->right->year<<" "<<root->right->no<<" "<< root->right->data<<" "<<root->right->companyName<<endl;
    root->left=new AVLNode(2011,2,"Ayse Isik",'C');
    cout<<"Left: "<<root->left->year<<" "<<root->left->no<<" "<< root->left->data<<" "<<root->left->companyName<<endl;
    root->left->left=new AVLNode(2008,2,"Elif Ipek",'B');
    cout<<"Left: "<<root->left->left->year<<" "<<root->left->left->no<<" "<< root->left->left->data<<" "<<root->left->left->companyName<<endl;
*/
    system("pause");
    return 0;
}